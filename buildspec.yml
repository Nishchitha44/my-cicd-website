version: 0.2

phases:
  build:
    commands:
      - echo "Starting deployment..."
      - aws s3 sync . s3://YOUR-BUCKET-NAME --delete --exclude "buildspec.yml" --exclude ".git/*"
      - echo "Deployment complete!"